- name: Create VM from ISO
  community.vmware.vmware_guest:
    hostname: "{{ vcenter_hostname }}"
    username: "{{ vcenter_user }}"
    password: "{{ vcenter_pass }}"
    validate_certs: no

    datacenter: "LAB Datacenter"

    # ✅ USE EITHER CLUSTER OR ESXI HOST (this is safer)
    cluster: "LAB Cluster"
    resource_pool: "Resources"

    folder: "/LAB Datacenter/vm"

    name: "{{ hostname }}"
    guest_id: windows9Server64Guest

    state: poweredon

    hardware:
      memory_mb: "{{ (memory | int) * 1024 }}"
      num_cpus: "{{ cpu | int }}"
      scsi: paravirtual
      hotadd_cpu: false           # ✅ FIX
      hotadd_memory: false        # ✅ FIX

    disk:
      - size_gb: "{{ disk_size | int }}"
        type: thin
        datastore: "LABVMW_DATASTORE"
        scsi_controller: 0        # ✅ FIX

    cdrom:
      - type: iso
        iso_path: "[Iso Data Store] vmware_iso/Windows10.iso"

    networks:
      - name: "LABServerPortGroup"